# ============================================================
# MAJI — Dépendances
# ============================================================

# Backend API
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.9

# Vision IA — Google Gemini (provider principal)
google-generativeai==0.8.6

# Vision IA — OpenAI GPT-4o (provider alternatif, optionnel)
openai==2.36.0

# OCR (optionnel — chargement lazy, non bloquant si absent)
# paddleocr==2.7.3
# paddlepaddle==2.6.1

# Computer Vision & Traitement d'image
opencv-python-headless==4.9.0.80
Pillow==10.3.0
numpy==1.26.4

# PDF
PyMuPDF==1.24.3

# Configuration & Validation
pydantic==2.13.4
python-dotenv==1.0.1

# HTTP
httpx==0.27.0

# Frontend
streamlit==1.35.0
pandas==2.2.2
openpyxl==3.1.4

# Tests
pytest==8.2.0
pytest-asyncio==0.23.7
